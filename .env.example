# Supabase project settings (Project Settings -> API in the Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Used by scripts/import-seed.ts to load lookup/chapter/rubric
# seed data, bypassing RLS. Never expose this to the browser or commit it.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
